[package]
name = "sync-brewfile"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "sync-brewfile"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
