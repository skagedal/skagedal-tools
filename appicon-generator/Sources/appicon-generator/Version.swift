/// Reported by `--version`.
///
/// Hand-maintained. The pre-monorepo tool generated this from a git tag with a
/// rake task, but monorepo tools are installed from source with ./install and
/// are not tagged or released individually.
let appIconGeneratorVersion = "2.0.0"
