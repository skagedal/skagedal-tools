# codestyle

Conventions that apply across repos, not just this one. `AGENTS.md` says
what to do inside `skagedal-tools`; these documents say how things are
done generally, so they can be linked to from elsewhere.

| Document | What it covers |
|----------|----------------|
| [dependency-versions.md](dependency-versions.md) | Why JavaScript manifests write `"latest"` for every dependency, and when not to |
